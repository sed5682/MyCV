@model IEnumerable<CvAssignment.ViewModel.ViewUsers>

@{
    ViewBag.Title = "ViewUsers";
}

<h2>ViewUsers</h2>

<p>
    @Html.ActionLink("Create New", "CreateNewUser")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Full_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Other_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
    
        <td>
            @Html.DisplayFor(modelItem => item.Full_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Other_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditPersonalDetails", new { id=item.Person_ID }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
